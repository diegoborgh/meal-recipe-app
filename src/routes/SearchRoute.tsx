/** "/search" — Filters, results, infinite scroll. Lands in slice 2. */
export function SearchRoute() {
  return (
    <section style={{ padding: 24 }}>
      <h1 className="t-display-lg">Search</h1>
      <p className="t-caption">Slice 2 placeholder.</p>
    </section>
  );
}
